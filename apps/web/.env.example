# URL base do backend (obrigatório em produção)
# Em dev, deixar vazio — o Vite proxy cuida do redirecionamento
# Em produção (Vercel), configurar como variável de ambiente de build:
#   VITE_API_URL=https://whatsappai-api-u61i.onrender.com
VITE_API_URL=
